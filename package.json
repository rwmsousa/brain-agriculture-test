{
  "name": "brain-agriculture",
  "version": "1.0.0",
  "description": "Brain Agriculture — monorepo fullstack (NestJS + React)",
  "private": true,
  "scripts": {
    "dev": "make dev",

    "test": "npm run test --prefix backend && npm run test --prefix frontend",
    "test:cov": "npm run test:cov --prefix backend && npm run test:cov --prefix frontend",

    "sonar:backend": "make sonar-backend",
    "sonar:frontend": "make sonar-frontend",
    "sonar": "make sonar",

    "lint": "npm run lint --prefix backend && npm run lint --prefix frontend",
    "format": "npm run format --prefix backend && npm run format --prefix frontend"
  }
}
